import React from 'react';
import { useSelector, useDispatch } from 'react-redux';
import { logout } from '../store';

const Home = ()=> {
  return (
    <div>
      <h1>Description</h1>
    </div>
  );
};

export default Home;
